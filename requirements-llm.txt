# Optional dependencies for built-in LLM agent harness
# pip install -r requirements-llm.txt

pyyaml>=6.0
duckduckgo-search>=6.0

# Optional: official SDKs (harness uses stdlib urllib by default)
# openai>=1.0.0
# anthropic>=0.25.0
