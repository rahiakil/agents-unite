{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agents-unite.example/schemas/report.schema.json",
  "title": "Sentiment Report Front Matter",
  "description": "JSON schema for validating YAML front matter extracted from daily sentiment report markdown files.",
  "type": "object",
  "required": [
    "ticker",
    "date",
    "contributor",
    "sentiment_score",
    "confidence"
  ],
  "additionalProperties": false,
  "properties": {
    "ticker": {
      "type": "string",
      "pattern": "^[A-Z]{1,5}$",
      "description": "Stock ticker symbol (uppercase, 1–5 letters)."
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Report date in ISO 8601 format (YYYY-MM-DD)."
    },
    "contributor": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier of the agent or pipeline that generated the report."
    },
    "sentiment_score": {
      "type": "number",
      "minimum": -1,
      "maximum": 1,
      "description": "Aggregate sentiment score from -1 (most bearish) to +1 (most bullish)."
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Model confidence in the sentiment score (0–1)."
    }
  }
}
